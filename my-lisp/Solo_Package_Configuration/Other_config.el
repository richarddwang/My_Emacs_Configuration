;;; Other_config.el --- other solo package but don't have a lot confg, not need to be elisp file

;;; Commentary:
;; Solo_pacakge_configuration means configuration of packages that play solo to function, i.e. don't belong to any specified function category and have some variables to adjust not just to bind key or other minor configuration

;;; Code:

(use-package hydra)

(provide 'Other_config)
;;; Other_config.el ends here
